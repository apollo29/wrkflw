# PHP / Composer
/vendor/
/backend/vendor/
# Root-Lock wird nicht benoetigt (Library; backend/composer.lock bleibt getrackt)
/composer.lock

# IDE
/.idea/

# Lokale npm-Auth (GitHub Packages) — enthaelt ggf. einen Token, nie committen.
# Vorlage: .npmrc.example
.npmrc
/backend/.phpunit.cache/
/backend/.phpstan.cache/
/backend/.php-cs-fixer.cache

# Node / Angular
node_modules/
/frontend/dist/
/frontend/.angular/

# Umgebung & lokale Konfiguration
.env
.env.local

# Referenz-Scaffold (wird Phase fuer Phase test-getrieben eingebaut)
/workflow-engine.zip

# OS / Editor
.DS_Store
Thumbs.db
*.log
